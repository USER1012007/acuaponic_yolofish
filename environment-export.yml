name: fishbowl-export
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - ultralytics
      - onnx
      - onnxruntime
      - numpy
      - opencv-python
      # hailo-dataflow-compiler se instala manualmente desde el SDK de Hailo
      # ver docs/hailo_setup.md
